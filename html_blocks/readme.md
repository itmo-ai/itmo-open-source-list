### Первый блок (html-block) прямо перед главным(который отвечает за превью).
Скрывает кнопку меню(которая выглядит как три горизонтальные полосы на мобильной версии)
Обычно эта кнопка появлялась на десктопной версии на 1-3 секунды при перезагрузки страницы
Файл `invise_menu_button.html`
Нужно заменить айди блока в `#rec1820665561`(соответственно заменить айди ещё в 4-ёх строках) на тот,
который будет в новом блоке(удобно найти через `ctrl+shift+c`, навестись на первый блок)

### Второй блок для плашки "проекты", соответственно перед блоком с плашкой
Нужен для того, чтобы плашка "проекты" в zero-block, где находится текст:
"Открытые проекты В ИТМО создается множество открытых проектов..." был скролл это этой **плашки**
и также элемент фиксации в конце(а именно кнопки "пишите" и "репозиторий")
Файл `for_object(project).html`

### Третий блок после блоков (аналитика и научпоп+кроме того), для отображения фона
Файл `background_science-oriented.html`
Добавить строку  `#recАйди .t396__artboard`, (айди также найти через `ctrl+shift+c`,
навестись на блок "аналитика и научпоп")